Fonzo.setup do |config|
	
  # config.ensure_canvas_app do |f|
  #   f[:paths] = %w(/canvas /canvas/*path)
  #   f[:redirect] = 'https://apps.facebook.com/<%= Rails.application.class.name.split("::").first.underscore.dasherize %>'
  # end
  # 
  # config.ensure_tab_app do |f|
  #   f[:paths] = %w(/tab /tab/*path)
  #   f[:redirect] = 'http://www.facebook.com/pages/<%= Rails.application.class.name.split("::").first.underscore.dasherize %>'
  # end
  # 
  # config.ensure_liked do |f|
  #   f[:paths] = %w(/tab /tab/*path)
  #   f[:route] = 'pages#show'
  #   f[:params] = { :id => 'like'}
  # end  
  # 
  # config.ensure_registered do |f|
  #   f[:paths] = %w(/*path)
  #   f[:route] = 'pages#show'
  #   f[:params] = { :id => 'register'}
  # end
  
end